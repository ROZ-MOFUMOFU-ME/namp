# Real operator configuration (only the examples are committed)
config.json
pool_configs/*.json
coins/*.json
coins/regtest/
coins/testnet/

# Build artifacts (legacy TypeScript/Next.js experiments — not used)
dist/
.next/
*.tsbuildinfo

# Local logs
*.log
.bootlog
logs/

node_modules/
